---
# handlers file for netbox-restore

- name: restart netbox services
  systemd:
    name: "{{ item }}"
    state: restarted
    enabled: yes
    daemon_reload: yes
  with_items:
    - netbox
    - netbox-rq

- name: restart postgresql
  systemd:
    name: postgresql
    state: restarted
